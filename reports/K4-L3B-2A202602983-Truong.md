# Individual contribution report

## Thông tin

- Họ và tên: Mai Văn Trường
- Mã học viên: 2A202602983
- Nhóm: AIGAN
- Repository/branch: VietDuc005/K4-L3B-RAG-Pipeline (main)

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| **Task 4: Chunking & Indexing** | Xây dựng pipeline `load_documents`, `chunk_documents`, `embed_texts`, `embed_chunks`, `index_to_vectorstore`. Chia 10 tài liệu Markdown thành 628 chunks (chunk_size=500, overlap=50) có metadata `chunk_index` và lưu vào ChromaDB với cosine distance. | `src/task4_chunking_indexing.py` | Done |
| **Task 5: Dense Semantic Search** | Thực hiện `semantic_search` dùng `embed_texts` để vector hóa query, truy vấn ChromaDB, đổi cosine distance thành Similarity score `max(0.0, 1.0 - distance)`, trả về `SearchResult` chuẩn schema. | `src/task5_semantic_search.py` | Done |
| **Task 6: Lexical BM25 Search** | Xây dựng `build_bm25_index` và `lexical_search`. Thiết kế `CustomBM25` duy trì sàn IDF dương cho corpus nhỏ, gán `retrieval_method = "bm25"` đúng contract. | `src/task6_lexical_search.py` | Done |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Sử dụng `SentenceTransformer` (`all-MiniLM-L6-v2`) làm model embedding thống nhất với cơ chế lazy-loading (singleton).  
   **Lý do/evidence:** Đảm bảo hàm `embed_texts()` dùng chung cho cả Task 4 (Indexing) và Task 5 (Semantic Search) tạo ra không gian vector 384-dim đồng nhất, tốc độ xử lý nhanh, tự chủ và không phụ thuộc API key bên ngoài.  
   **Trade-off:** Vector 384-dim nhẹ hơn bge-m3 (1024-dim) nhưng bù lại tốc độ indexing và search cực kỳ tối ưu, chạy hoàn toàn offline và vượt qua tất cả unit test contract.

2. **Quyết định:** Định nghĩa lớp `CustomBM25` kế thừa `BM25Okapi` để duy trì sàn IDF dương (`max(idf, 0.1)`).  
   **Lý do/evidence:** Trong các bộ dữ liệu test nhỏ ($N=2$), công thức BM25Okapi mặc định tính $\text{IDF} = \ln(1.0) = 0.0$ cho từ khóa chỉ xuất hiện ở 1 trong 2 tài liệu, khiến score bị trả về 0.  
   **Trade-off:** Cần ghi đè hàm `_calc_idf` của `rank_bm25`, bù lại giúp BM25 xếp hạng chính xác các văn bản khớp từ khóa và giúp contract unit test `test_lexical_search_returns_bm25_contract` đạt 100% PASS.

## Kiểm thử và kết quả

- **Test hoặc query tôi đã dùng:**
  - `pytest tests/test_contracts.py -k "task4 or document or chunk or semantic or lexical"`
  - Chạy trực tiếp pipeline: `python -m src.task4_chunking_indexing`, `python -m src.task5_semantic_search`, `python -m src.task6_lexical_search`.
- **Kết quả trước/sau:**
  - *Trước*: Các hàm ném ngoại lệ `NotImplementedError`, test contract thất bại.
  - *Sau*: Đã nạp 10 tài liệu Markdown -> 628 chunks -> 628 vector embeddings trong ChromaDB. Tất cả 8/8 contract unit tests thuộc Tasks 4, 5, 6 đều PASSED.
- **Lỗi đã phát hiện và cách xử lý:**
  - *Lỗi 1*: BM25Okapi trả score = 0 cho tài liệu chứa từ khóa khi corpus nhỏ ($N=2$).  
    *Cách xử lý*: Định nghĩa `CustomBM25` ép sàn IDF tối thiểu 0.1.
  - *Lỗi 2*: Warning trùng lặp khai báo hằng số trong `task4_chunking_indexing.py`.  
    *Cách xử lý*: Dọn dẹp imports và tái cấu trúc hằng số chuẩn hóa ở đầu file.

## Điều còn hạn chế

- **Một hạn chế cụ thể của phần tôi làm:** BM25 hiện tại mới chỉ tokenize cơ bản theo khoảng trắng (`lower().split()`), chưa tích hợp tách từ chuyên sâu (lemmatization/stemming) hoặc xử lý n-gram cho tiếng Việt/tiếng Anh phức tạp.
- **Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện:** Tích hợp bộ tách từ tiếng Việt (Underthesea/PhoNLP) để tiền xử lý văn bản trước khi đưa vào BM25 nhằm nâng cao Context Recall cho tìm kiếm từ khóa.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 25/09/2026
- Tên thành viên: Mai Văn Trường
